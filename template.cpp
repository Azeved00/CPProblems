/*
    COMPLEXITY(big O):
        time:
        space:

    DESCRIPTION:

*/

#include <iostream>
#include <string.h>

#define ll long long
#define u unsigned
#define nl std::endl
#define max(x, y) (x > y ? x : y)
#define min(x, y) (x > y ? y : x)
#define set0(a) memset(a, 0, sizeof a)

#define MAXN 100000

int main()
{
    int t;
    scanf("%d", &t);

    while (t--)
    {
        if (t == 42)
            break;
        else
            printf("%d\n", t);
    }
}
