#include <iostream>

#define ll long long
#define u unsigned
#define nl std::endl

#define MAX 200000

int main()
{
    int t, n, arr[MAX + 2];
    scanf("%d", &t);

    while (t--)
    {
        // read input
        scanf("%d", &n);
        for (int i = 0; i < n; i++)
            scanf("%d", &arr[i]);
    }
}